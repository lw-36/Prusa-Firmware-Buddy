if(HAS_NEXTRUDER)
  target_sources(firmware PRIVATE standard_feedrates_nextruder.cpp)
elseif(HAS_INDX)
  target_sources(firmware PRIVATE standard_feedrates_indx.cpp)
elseif(PRINTER STREQUAL "MINI")
  target_sources(firmware PRIVATE standard_feedrates_mini.cpp)
elseif(PRINTER STREQUAL "MK3.5")
  target_sources(firmware PRIVATE standard_feedrates_mk3_5.cpp)
else()
  message(FATAL_ERROR "Missing standard feedrates setup for \"${PRINTER}\".")
endif()
